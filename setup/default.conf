server{
    listen 80 default_server;
    server_name docs.orcfax.link;
    root /usr/share/nginx/html;

    location / {
        try_files $uri /index.html;
    }

    location /api {
        include uwsgi_params;
        uwsgi_pass unix:/opt/orcfax/asock.sock;
    }
}